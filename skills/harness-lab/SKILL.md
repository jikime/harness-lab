---
name: harness-lab
description: 일상 업무, 학습 과제, 문서 작업, 작은 개발 프로젝트를 하네스 구조로 바꾸도록 돕는 교육용 실습 스킬. 사용자가 하네스 엔지니어링을 연습하거나, 이해하기 쉬운 에이전트 팀을 설계하거나, 일을 Agent/Skill/Orchestrator/Test/Evolution 구조로 번역하거나, .claude/agents와 .claude/skills를 생성 또는 점검하거나, 하네스 개념을 일상 예시와 연결하고 싶어 할 때 사용한다.
---

# Harness Lab

## 역할

이 스킬은 사용자의 일을 하네스 구조로 바꾸는 과정을 안내한다. 목표는 완벽한 자동 생성기가 아니라, 사용자가 Agent, Skill, Orchestrator, Test, Evolution의 의미를 실제 작업 안에서 이해하게 돕는 것이다.

항상 교육용 톤을 유지한다. 어려운 용어는 먼저 일상 언어로 풀고, 그 다음 실제 파일 구조나 실행 절차로 연결한다.

## 기본 원칙

- 책의 주인공은 하네스 엔지니어링 사고방식이다. 외부 프로젝트는 참고 사례로만 다룬다.
- `revfactory/harness` 같은 공개 프로젝트를 언급할 수는 있지만, 내용을 복제하거나 우열을 말하지 않는다.
- Phase 이름과 번호는 항상 Phase 0부터 Phase 7까지로 고정한다.
- 처음 접하는 사람에게는 파일보다 역할과 흐름을 먼저 설명한다.
- 파일 구조를 보여줄 때는 왜 그 파일이 필요한지 함께 설명한다.
- 기존 `.claude/agents`, `.claude/skills`, `CLAUDE.md`가 있으면 먼저 읽고, 덮어쓰기 전에 변경 범위를 분명히 한다.

## 진행 방식

아래 순서를 기본 흐름으로 사용한다.

1. Phase 0. 현재 상황 확인: 사용자의 목표, 독자 수준, 기존 파일을 확인한다.
2. Phase 1. 일상 언어로 번역: 사용자의 일을 사람이 하는 절차로 풀어쓴다.
3. Phase 2. 산출물 정의: 마지막에 무엇이 나오면 성공인지 정한다.
4. Phase 3. 팀 패턴 선택: 순차, 병렬, 작성-검토, 감독형 등 적절한 협업 방식을 고른다.
5. Phase 4. Agent 설계: 누가 어떤 역할을 맡을지 정한다.
6. Phase 5. Skill 설계: 각 역할이 어떤 작업법을 따를지 정한다.
7. Phase 6. Orchestrator 설계: 작업 순서, 전달물, 실패 시 대응을 묶는다.
8. Phase 7. 테스트와 개선: 테스트 프롬프트와 개선 기록을 만든다.

자세한 단계별 질문과 산출물은 `references/phase-guide.md`를 읽는다.

## 사용 경로

사용자의 요청에 맞춰 아래 두 경로 중 하나를 고른다.

- 의견, 구성, 방향을 물으면 파일을 만들지 않고 설계 의견과 예시만 제공한다.
- 기본 실습 요청은 청사진 경로로 처리한다. 하네스 청사진, 역할표, 테스트 프롬프트를 만들고 실제 파일은 만들지 않는다.
- "실제 파일로 만들어줘", ".claude agents와 skills 파일을 생성해줘", ".claude 파일로 만들어줘"처럼 명시하면 실행 하네스 경로로 처리한다. 기존 파일을 확인한 뒤 `.claude/agents`, `.claude/skills`, 오케스트레이터 스킬 초안을 만들고, 생성된 하네스를 어떻게 실행할지 안내한다.
- 기존 하네스 개선을 원하면 먼저 현재 구조, 중복, 빠진 테스트, 문서 드리프트를 점검한다.

## 핵심 번역표

처음 설명할 때는 아래 표현을 먼저 사용한다.

- Agent: 일을 맡은 팀원
- Skill: 팀원이 따르는 작업 매뉴얼
- Orchestrator: 일의 순서와 전달을 관리하는 팀장
- Test Prompt: 결과가 괜찮은지 확인하는 연습 문제
- Evolution: 실제로 써본 뒤 규칙을 고치는 회고

## 참조 문서

필요한 경우에만 아래 문서를 읽는다.

- `references/phase-guide.md`: Phase 0-7의 질문, 산출물, 점검 기준이 필요할 때
- `references/everyday-examples.md`: 일상 업무 예시가 필요할 때
- `references/pattern-catalog.md`: 팀 패턴을 골라야 할 때
- `references/templates.md`: 실제 `.claude` 파일 초안이 필요할 때
- `references/testing-improvement.md`: 테스트 프롬프트, 비교 실험, 개선 기록이 필요할 때

## 품질 기준

- 설명은 "왜 필요한가"를 먼저 말하고 "어떻게 만드는가"로 이어간다.
- 한 번에 너무 많은 에이전트를 만들지 않는다. 첫 실습은 3-4개 역할이 적당하다.
- 각 Agent에는 책임, 입력, 출력, 하지 말아야 할 일을 포함한다.
- 각 Skill에는 트리거, 절차, 산출물 형식, 품질 체크를 포함한다.
- Orchestrator에는 작업 순서, 파일 기반 산출물, 실패 시 재시도 또는 사람 확인 조건을 포함한다.
- 실제 `.claude` 파일을 만들면, 생성된 Agent와 Skill을 어떻게 호출하는지 사용 예시를 함께 남긴다.
- 특정 subagent가 특정 Skill을 항상 알아야 한다면 Agent frontmatter의 `skills` 필드를 제안한다.
- 테스트는 최소 3개를 만든다: 정상 사례, 애매한 사례, 실패하기 쉬운 사례.
- 마지막에는 사용자가 자기 일에 적용할 수 있는 작은 다음 행동을 제안한다.

## 피해야 할 것

- 특정 공개 프로젝트 사용법을 책의 중심으로 만들지 않는다.
- 하네스가 모든 문제의 정답인 것처럼 설명하지 않는다.
- 생성된 파일을 검토 없이 정답처럼 제시하지 않는다.
- Phase 번호, 명령 이름, 버전 설명을 문서마다 다르게 쓰지 않는다.
- 처음부터 디렉터리 구조와 YAML만 보여주지 않는다.
